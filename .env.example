# 有氧健嘴房 · 环境变量说明

复制本文件为 `.env` 并填入真实值（`.env` 已被 `.gitignore` 忽略，不会提交）。

## 服务端（Vercel Serverless Functions 使用）

| 变量名 | 必填 | 说明 |
|---|---|---|
| `DEEPSEEK_API_KEY` | 是 | DeepSeek 开放平台 API Key，仅在后端使用，绝不暴露给浏览器 |
| `DEEPSEEK_BASE_URL` | 否 | 默认 `https://api.deepseek.com`，一般无需修改 |
| `DEEPSEEK_MODEL` | 否 | 默认 `deepseek-chat` |

## 本地开发

```bash
cp .env.example .env
# 然后编辑 .env，把 your_key_here 换成你的真实 Key
npm run dev
```

## Vercel 部署

在 Vercel Dashboard → 项目 Settings → Environment Variables 中添加
`DEEPSEEK_API_KEY`（值为你自己的 Key），不要把 Key 提交进代码仓库。
